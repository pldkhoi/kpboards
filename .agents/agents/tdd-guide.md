# TDD Guide Agent (Mirror)

Source: `.cursor/agents/tdd-guide.md`

Use when:
- implementing features or bug fixes test-first
- strengthening test coverage around risky refactors

Codex adaptation:
- use Vitest with Bun commands:
  - `bun run test`
  - `bun run test:coverage`
