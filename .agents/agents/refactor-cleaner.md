# Refactor Cleaner Agent (Mirror)

Source: `.cursor/agents/refactor-cleaner.md`

Use when:
- dead code, duplicate utilities, or stale exports need cleanup

Codex adaptation:
- run `bun run knip` first
- remove in small batches with verification (`type-check` + tests) between batches
