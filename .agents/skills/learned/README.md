# Learned Skills

Store session-derived reusable patterns here:

- Path: `.agents/skills/learned/<pattern-name>/SKILL.md`
- Format: standard Codex skill with YAML frontmatter (`name`, `description`)
- Scope: one pattern per skill, with clear trigger conditions
