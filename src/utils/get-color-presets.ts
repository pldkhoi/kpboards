// Re-export from standalone color presets (no MUI dependency)
export {
  amberPreset,
  colorPresets,
  cyanPreset,
  default,
  defaultPreset,
  emeraldPreset,
  fuchsiaPreset,
  indigoPreset,
  orangePreset,
  rosePreset,
  skyPreset,
  slatePreset,
  tealPreset,
  violetPreset,
} from './color-presets';
